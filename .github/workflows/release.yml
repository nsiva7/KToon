name: Create Release

on:
  push:
    branches:
      - main
    paths:
      - 'build.gradle.kts'
  workflow_dispatch:

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      should-release: ${{ steps.check-release.outputs.should-release }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version from build.gradle.kts
        id: get-version
        run: |
          VERSION=$(grep '^version = ' build.gradle.kts | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Found version: $VERSION"

      - name: Check if release exists
        id: check-release
        run: |
          VERSION=${{ steps.get-version.outputs.version }}
          if gh release view "v$VERSION" >/dev/null 2>&1; then
            echo "Release v$VERSION already exists"
            echo "should-release=false" >> $GITHUB_OUTPUT
          else
            echo "Release v$VERSION does not exist"
            echo "should-release=true" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-and-test:
    runs-on: ubuntu-latest
    needs: check-version
    if: needs.check-version.outputs.should-release == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Run tests
        run: ./gradlew test

      - name: Build library
        run: ./gradlew build

      - name: Generate documentation
        run: ./gradlew dokkaHtml

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            build/libs/*.jar
            build/dokka/html/**

  create-release:
    runs-on: ubuntu-latest
    needs: [check-version, build-and-test]
    if: needs.check-version.outputs.should-release == 'true'
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: build/

      - name: Generate changelog
        id: changelog
        run: |
          VERSION="${{ needs.check-version.outputs.version }}"
          
          # Get the latest tag (if any)
          LATEST_TAG=$(git tag --sort=-version:refname | head -n1)
          
          if [ -z "$LATEST_TAG" ]; then
            # First release
            CHANGELOG="## 🎉 Initial Release

This is the first release of KToon - Enhanced Kotlin implementation of TOON format with full encode/decode support.

### ✨ Features
- 🎯 **Complete TOON Implementation**: Both encoding and decoding support
- 💾 **Type-Safe Conversions**: Decode to Kotlin data classes with reified generics
- 🔄 **Bidirectional**: Seamless conversion between TOON, JSON, and Kotlin objects
- 📊 **Tabular Arrays**: Efficient handling of structured data
- ⚙️ **Configurable Options**: Custom delimiters, indentation, and length markers
- ☕ **Java Interop**: Full compatibility with Java projects
- 🧪 **Comprehensive Tests**: 45+ test cases with 100% round-trip validation

### 📦 Installation
\`\`\`kotlin
dependencies {
    implementation(\"com.github.nsiva7:KToon:v$VERSION\")
}
\`\`\`

### 🚀 Quick Start
\`\`\`kotlin
// Encoding
val toonString = KToon.encode(myObject)

// Decoding
val myObject: MyClass = KToon.decode(toonString)
\`\`\`"
          else
            # Generate changelog from commits since last tag
            CHANGELOG="## 🔄 Changes since $LATEST_TAG

$(git log $LATEST_TAG..HEAD --pretty=format:'- %s' --no-merges)

### 📦 Installation
\`\`\`kotlin
dependencies {
    implementation(\"com.github.nsiva7:KToon:v$VERSION\")
}
\`\`\`"
          fi
          
          echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          tag: "v${{ needs.check-version.outputs.version }}"
          name: "KToon v${{ needs.check-version.outputs.version }}"
          body: ${{ steps.changelog.outputs.CHANGELOG }}
          artifacts: "build/libs/*.jar"
          generateReleaseNotes: true
          makeLatest: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment on commit
        run: |
          VERSION="${{ needs.check-version.outputs.version }}"
          gh api repos/${{ github.repository }}/commits/${{ github.sha }}/comments \
            --method POST \
            --field body="🎉 Released as [v$VERSION](https://github.com/${{ github.repository }}/releases/tag/v$VERSION)

📦 **Installation:**
\`\`\`kotlin
implementation(\"com.github.nsiva7:KToon:v$VERSION\")
\`\`\`

🔗 **JitPack:** https://jitpack.io/#nsiva7/KToon/v$VERSION"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}